# 像素动画 IDE 项目文档

**版本**：v0.1   
**技术栈**：Python + PySide6

---

## 目录

1. [项目概述](#1-项目概述)
2. [目标用户与使用场景](#2-目标用户与使用场景)
3. [功能需求](#3-功能需求)
4. [非功能需求](#4-非功能需求)
5. [技术选型](#5-技术选型)
6. [系统架构](#6-系统架构)
7. [目录结构](#7-目录结构)
8. [核心模块设计](#8-核心模块设计)
9. [开发阶段计划](#9-开发阶段计划)
10. [关键算法说明](#10-关键算法说明)
11. [风险与应对](#11-风险与应对)
12. [附录：预设提示词库](#12-附录预设提示词库)

---

## 1. 项目概述

**像素动画 IDE** 是一个面向游戏开发者、独立开发者和像素美术爱好者的桌面端工具。它集成多种 AI 能力与本地图像处理算法，打通从“文本描述 → 图片生成 → 动画生成 → 严格像素化 → 背景去除 → 序列帧导出”的完整工作流，大幅降低像素动画制作门槛。

本工具提供两种使用模式：

- **Solo 模式**：一键式生成，用户仅需输入简单参数，系统自动完成全部流程。
- **IDE 模式**：分步式专业工作区，各流程可独立操作、编辑和导出，适合需要精细控制的用户。

### 1.1 核心特色

- 支持用户自行配置三种 AI API（通用文本、图片生成、图转视频），降低使用成本。
- 内置确定性像素化算法，将不完美像素转换为严格像素风格。
- 支持序列帧编辑、像素级修改、多种动画预览速度。
- 导出多种交付格式：GIF、PNG 序列帧、雪碧图、APNG、JSON 元数据。
- 界面风格参考 Codex，简洁专业，支持深色/浅色主题。

### 1.2 术语定义

- **通用 API**：用于聊天、文本生成、提示词生成的 LLM API。
- **生图 API**：用于根据文本提示词生成图片的 API。
- **图转视频 API**：以一张图片作为首帧，根据提示词生成指定帧数/时长的视频/帧序列的 API。
- **Solo 模式**：一站式生成模式，用户仅需设置少量参数，系统自动完成全流程。
- **IDE 模式**：分步式专业模式，各流程分离，用户可逐步操作、编辑和导出。
- **严格像素/像素化处理**：通过确定性算法将任意图片/视频帧转换为符合像素美术规范的结果，包括像素网格对齐、色彩量化、边缘清理、帧间一致性处理等，**不依赖 AI**。

---

## 2. 目标用户与使用场景

| 用户类型 | 使用场景 |
|---------|----------|
| 游戏开发者 | 快速生成角色、怪物、特效等像素动画资源 |
| 独立开发者 | 在缺乏专业动画师的情况下，通过 AI 辅助完成动画制作 |
| 像素美术创作者 | 利用生成结果作为初稿，进一步进行像素级精修 |
| 原型验证人员 | 快速产出动画素材用于玩法验证或 Demo 展示 |

---

## 3. 功能需求

### 3.1 设置与 API 管理

系统提供设置页，支持用户配置各工作流中需要的 API。

#### 3.1.1 通用文本 API
- 用途：聊天、文本生成、提示词生成。
- 可配置项：Base URL、API Key、模型名称、temperature、max_tokens、top_p、超时时间。

#### 3.1.2 图片生成 API
- 用途：根据提示词生成图片。
- 可配置项：Base URL、API Key、模型名称、默认分辨率/宽高比、采样步数、种子等。

#### 3.1.3 图转视频 API
- 用途：以图片作为首帧，根据提示词生成指定帧数/时长的视频或帧序列。
- 可配置项：Base URL、API Key、模型名称、默认帧数范围、提示词格式模板、是否返回帧序列或仅返回视频。

#### 3.1.4 API 管理通用能力
- 支持保存多套配置，并可切换默认配置。
- 支持 API 连通性测试。
- API Key 本地加密存储（使用 `cryptography` 库）。
- 支持导入/导出配置文件（JSON）。

### 3.2 Solo 模式

#### 输入参数
- **文本描述**：用户对角色、动作、风格等的自然语言描述。
- **分辨率/宽高比**：目标输出分辨率或比例。
- **帧数或时长**：目标动画帧数或视频时长。
- **动作类型/动画提示词**（可选）。
- **像素化强度、背景去除方式**（可选，提供默认值）。

#### 自动流程
1. 根据文本描述生成专业详尽的图片提示词和动画提示词（调用通用 API）。
2. 根据图片提示词生成首帧图片（调用生图 API）。
3. 以首帧图片和动画提示词生成视频/帧序列（调用图转视频 API）。
4. 对生成结果进行像素化处理（本地算法）。
5. 去除背景（默认白色背景，可配置颜色键）。
6. 导出 GIF、序列帧图等交付物。

#### 交互与反馈
- 显示总体进度和当前步骤状态。
- 支持任务失败后重试，并保留已生成的中间产物。
- 提供日志查看功能，便于排查 API 调用问题。

### 3.3 IDE 模式

IDE 模式提供更完整的工作流界面，各流程分离，用户可通过左侧引导栏选择步骤逐步操作。

#### 3.3.1 界面布局
- **左侧**：步骤引导栏 / 资源树。
- **中间**：主工作区，用于图片/动画预览或帧编辑。
- **右侧**：参数面板 / 属性面板。
- **底部**：时间轴、日志或任务进度区域。
- 整体风格参考 Codex，简洁、专业，支持深色/浅色主题（QSS 实现）。

#### 3.3.2 文本生成
- 输入：用户自然语言描述。
- 输出：专业图片提示词、动画提示词、可选负面提示词。
- 支持用户手动编辑生成结果。
- 支持保存和复用提示词模板。

#### 3.3.3 图片生成
- 输入：提示词、宽高比/分辨率、风格参数（如像素风、角色设定等）。
- 输出：一张或多张候选图片。
- 支持严格按目标分辨率生成；若 API 不支持，应明确提示或进行裁剪/填充。
- 支持重新生成、选择候选图、局部微调（可选）。

#### 3.3.4 视频动画生成
- 输入：首帧图片、提示词或预设提示词、帧数或秒数、帧率（如 8/12/24 fps）。
- 预设提示词分类：
  - **移动类**：步行、奔跑、跳跃、突进、爬行。
  - **战斗类**：攻击、格挡、昏迷。
  - 支持用户自定义并扩展预设提示词。
- 输出：对应帧数的视频或帧序列。
- 若 API 仅返回视频，系统应自动拆分为帧序列。

#### 3.3.5 像素化与背景去除
- **像素化处理**（本地算法，详见 [关键算法说明](#10-关键算法说明)）：
  - 对生成视频/帧序列进行像素风格化处理。
  - 可选处理方式：降采样、色彩量化、边缘锐化、调色板限制等。
  - 保证帧间一致性。
- **背景去除**：
  - 默认去除白色背景。
  - 支持自定义颜色键、容差设置。
  - 输出带透明通道的帧序列。
  - 支持去除背景后边缘清理（形态学操作）。

### 3.4 序列帧编辑

IDE 模式下提供针对序列帧的编辑能力。

#### 帧列表与时间轴
- 以时间轴或帧列表形式展示所有帧。
- 支持拖动调整帧顺序。
- 支持选择单帧或多帧操作。

#### 预览
- 支持以不同帧率预览动画（如 1x、2x、循环播放、逐帧步进）。
- 支持倍速播放。

#### 帧操作
- 删除帧、插入新帧、复制帧、移动帧顺序。
- 导入外部帧图片并插入到当前序列。

#### 像素级编辑
- 进入单帧编辑画布，支持像素点编辑。
- 基础工具：铅笔、橡皮、取色、填充、矩形选择。
- 支持调色板约束，限制使用指定颜色。
- 支持撤销/重做。
- 支持洋葱皮显示，便于对齐相邻帧。

### 3.5 导出

- 支持导出格式：
  - GIF 动画。
  - PNG 序列帧（带透明通道）。
  - Sprite Sheet / 雪碧图。
  - APNG / WebP（可选）。
  - JSON 元数据（帧率、尺寸、帧数、锚点信息等）。
- 可配置项：帧率、循环次数、缩放比例、背景是否透明、文件命名规则、输出目录。

---

## 4. 非功能需求

### 4.1 性能
- AI 调用和图像处理均为耗时操作，需异步执行，避免阻塞 UI。
- 高分辨率帧处理时应提供进度反馈。
- 支持取消正在执行的任务。

### 4.2 稳定性与容错
- API 调用失败、超时、返回异常时应给出明确错误提示。
- 支持任务重试和断点续跑。
- 自动保存用户工作区状态，避免意外丢失。

### 4.3 安全与隐私
- API Key 加密存储。
- 用户素材和生成结果默认本地存储，未经用户允许不上传至任何服务器。

### 4.4 可扩展性
- API 类型、预设提示词、像素化算法、导出格式等应具备可扩展性。
- 预留插件或自定义脚本接口（可选）。

### 4.5 跨平台
- 目标平台：Windows / macOS / Linux 桌面端。
- 使用 PySide6 实现跨平台兼容。

---

## 5. 技术选型

### 5.1 语言与框架

- **语言**：Python 3.10+
- **GUI 框架**：PySide6（Qt 6）
- **异步与多线程**：`QThreadPool` + `QRunnable`，或 `qasync` + `asyncio`
- **图像处理**：`Pillow`、`OpenCV`、`numpy`
- **视频处理**：`imageio`、`imageio-ffmpeg`、`ffmpeg-python`
- **HTTP 请求**：`httpx`（支持同步/异步）
- **配置存储**：`QSettings` 或 JSON 文件
- **加密**：`cryptography`（用于 API Key 加密）
- **打包**：PyInstaller 或 Nuitka

### 5.2 选型理由

| 需求点 | Python + PySide6 优势 |
|--------|------------------------|
| AI API 调用 | 生态成熟，`httpx` 等库使用简单 |
| 图像处理 | Pillow/OpenCV/numpy 功能强大，满足像素化、去背景、调色板限制、GIF 生成等需求 |
| 视频/帧处理 | imageio/ffmpeg 可轻松完成视频拆帧、帧序列合成 |
| 像素级编辑 | Qt 的 `QGraphicsView` 适合实现像素画布、洋葱皮、调色板等编辑器功能 |
| 跨平台 | PySide6 支持 Windows/macOS/Linux |
| 开发效率 | Python 语法简洁，迭代速度快 |
| 界面风格 | Qt QSS 可模拟 Codex 风格 |
| 打包分发 | PyInstaller 可打包为独立可执行文件 |

---

## 6. 系统架构

### 6.1 总体架构

系统采用分层架构，分为 UI 层、工作流层、核心处理层和 API 抽象层。

```
+----------------+     +----------------+     +----------------+
|    UI 层        | --> |  工作流层       | --> |  核心处理层     |
| (PySide6)       |     | (workflow)     |     | (processing)   |
+----------------+     +----------------+     +----------------+
        |                      |                      |
        v                      v                      v
+----------------+     +----------------+     +----------------+
|   API 抽象层    |     |   存储层        |     |   工具层        |
| (api)          |     | (storage)      |     | (utils)        |
+----------------+     +----------------+     +----------------+
```

- **UI 层**：负责用户交互、页面展示、事件处理。不包含业务逻辑。
- **工作流层**：组织各步骤的执行顺序，管理任务状态、进度、取消等。
- **核心处理层**：实现像素化、背景去除、帧提取与合成等确定性算法。
- **API 抽象层**：统一封装第三方 API 调用，提供测试和错误处理。
- **存储层**：管理配置、API Key 加密、项目文件保存/加载。
- **工具层**：通用辅助函数、常量定义。

### 6.2 数据流

#### Solo 模式数据流
```
用户输入文本描述
    │
    ▼
[通用 API] 生成图片提示词 + 动画提示词
    │
    ▼
[生图 API] 生成首帧图片
    │
    ▼
[图转视频 API] 生成视频/帧序列
    │
    ▼
[像素化算法] 严格像素化
    │
    ▼
[背景去除] 透明背景
    │
    ▼
[导出] GIF / 序列帧 / 雪碧图
```

#### IDE 模式数据流
各步骤独立触发，中间结果可编辑，流程可回溯。

---

## 7. 目录结构

```
pixel_anim_ide/
├── main.py                     # 程序入口
├── requirements.txt            # 依赖清单
├── config/
│   ├── settings.py             # 全局配置（路径、常量）
│   └── api_config.json         # API 配置（加密后存储）
├── core/
│   ├── api/
│   │   ├── base.py             # API 基类
│   │   ├── llm_api.py          # 通用文本 API
│   │   ├── image_api.py        # 生图 API
│   │   └── video_api.py        # 图转视频 API
│   ├── workflow/
│   │   ├── solo_workflow.py    # Solo 模式全自动流程
│   │   └── ide_workflow.py     # IDE 模式分步流程
│   ├── processing/
│   │   ├── pixelizer.py        # 像素化处理
│   │   ├── background.py       # 背景去除
│   │   ├── frame_utils.py      # 帧提取、合成、GIF 生成
│   │   └── prompt_utils.py     # 提示词生成与预设
│   └── storage/
│       ├── project.py          # 项目文件保存/加载
│       └── keyring.py          # API Key 加密存储
├── ui/
│   ├── main_window.py          # 主窗口
│   ├── styles/
│   │   ├── dark.qss           # 深色主题
│   │   └── light.qss          # 浅色主题
│   ├── pages/
│   │   ├── settings_page.py    # 设置页
│   │   ├── solo_page.py        # Solo 模式页
│   │   └── ide_page.py         # IDE 模式页
│   ├── widgets/
│   │   ├── image_viewer.py     # 图片查看器
│   │   ├── frame_editor.py     # 帧编辑器（像素画布）
│   │   ├── timeline.py         # 时间轴/帧列表
│   │   └── api_config_widget.py# API 配置控件
│   └── dialogs/
│       └── export_dialog.py    # 导出对话框
├── assets/                     # 图标、预设提示词等资源
├── tests/                      # 单元测试
└── build/                      # 打包脚本（PyInstaller）
```

---

## 8. 核心模块设计

### 8.1 API 封装层（`core/api`）

**职责**：统一封装三种 API 的调用，屏蔽不同服务商的差异。

- 定义抽象基类 `BaseAPI`，包含 `test_connection()`、`call()` 等接口。
- 每个具体 API 类实现参数组装、请求发送、错误处理。
- 使用 `httpx` 发送请求，支持同步/异步。
- 返回统一的结果对象（成功/失败、数据、错误信息）。
- 支持超时、重试、日志记录。

### 8.2 工作流引擎（`core/workflow`）

**职责**：组织执行顺序，管理任务状态、进度、取消。

- Solo 工作流：串联“提示词生成 → 生图 → 图转视频 → 拆帧 → 像素化 → 去背景 → 导出”。
- IDE 工作流：提供每一步的独立入口，允许用户中断、编辑、重新执行。
- 实现：使用状态机或步骤队列，每一步作为独立任务，可报告进度、可取消。

### 8.3 图像/视频处理（`core/processing`）

- **pixelizer.py**：实现严格像素化算法（详见 [关键算法说明](#10-关键算法说明)）。
- **background.py**：背景去除，支持颜色键、容差、边缘清理。
- **frame_utils.py**：视频拆帧、帧序列合成 GIF/视频、雪碧图生成。
- **prompt_utils.py**：预设动作提示词库、模板渲染、提示词管理。

### 8.4 UI 层（`ui`）

- **main_window.py**：主窗口，管理侧边栏导航、页面切换。
- **Solo 页面**：表单输入 + 进度显示 + 日志输出。
- **IDE 页面**：左侧步骤栏，中间工作区，右侧属性面板，底部时间轴。
- **frame_editor.py**：像素级编辑画布，基于 `QGraphicsView`。
- **timeline.py**：缩略图列表，支持拖拽排序、删除、插入。
- **api_config_widget.py**：API 配置表单组件，可复用。

### 8.5 存储与配置

- **keyring.py**：使用 `cryptography` 对 API Key 进行加密存储。
- **project.py**：保存/加载项目文件（包含帧序列、设置、中间产物路径等）。

---

## 9. 开发阶段计划

### 阶段 1：MVP（最小可行产品）

**目标**：跑通 Solo 模式的最简链路。

1. 搭建主窗口和设置页，实现 API 配置的增删改查与连接测试。
2. 实现 `LLM API`、`Image API`、`Video API` 的基础调用。
3. 实现 Solo 工作流：输入文本 → 生成提示词 → 生图 → 图转视频 → 拆帧。
4. 实现简单的像素化（直接降采样 + 色彩量化）和白色背景去除。
5. 导出 PNG 序列帧和 GIF。

**验收标准**：用户配置好三个 API 后，输入一句话能自动生成一段像素动画 GIF。

**预计工期**：2~4 周。

### 阶段 2：IDE 模式基础

**目标**：实现分步操作和序列帧编辑。

1. 实现 IDE 页面布局，左侧步骤导航。
2. 将 Solo 工作流拆分为独立步骤，每步可单独执行和修改。
3. 实现时间轴和帧列表，支持预览、删除、插入。
4. 实现基本像素编辑画布（铅笔、橡皮、取色、填充）。

**验收标准**：用户可以在 IDE 中逐步生成，并在画布上修改任意帧的像素。

**预计工期**：4~8 周。

### 阶段 3：高级功能与优化

**目标**：完善导出、性能和用户体验。

1. 支持多种像素化算法和参数调节。
2. 增强背景去除效果（边缘处理、色键容差）。
3. 支持多候选图片选择、局部重绘（可选）。
4. 导出雪碧图、APNG、JSON 元数据。
5. 添加洋葱皮、调色板锁定、撤销重做。
6. 优化大尺寸帧的处理速度（多线程、缓存）。
7. 完善错误处理和日志。

**预计工期**：2~4 个月。

### 阶段 4：打包与发布

- 使用 PyInstaller 打包为 Windows/macOS/Linux 可执行文件。
- 处理依赖库体积，裁剪不必要的模块。
- 添加自动更新机制（可选）。

---

## 10. 关键算法说明

### 10.1 严格像素化算法（`pixelizer.py`）

**目标**：将源图片/视频帧转换为严格像素风格，消除抗锯齿、模糊边缘、杂色，统一到目标像素网格和调色板。

**输入**：
- 源图片/视频帧。
- 目标像素尺寸（如 64×64、128×128）。
- 调色板（可选，若无则自动提取）。
- 其他参数：色彩数量、边缘锐化程度等。

**输出**：严格像素风图片/帧。

#### 算法步骤

1. **像素网格对齐**
   - 使用最近邻插值（`Image.NEAREST`）将图像缩放到目标尺寸。
   - 若源图长宽比与目标不一致，先进行居中裁剪或填充，保持像素为正方形。

2. **色彩量化**
   - 若用户指定调色板，则将每个像素映射到调色板中最近的颜色（欧氏距离）。
   - 若未指定调色板，可自动提取调色板（K-means 聚类或中位切分），并限制颜色数量。
   - 支持固定调色板以确保序列帧颜色统一。

3. **去伪影与边缘清理**
   - 检测边缘过渡像素（处于前景与背景之间的中间色），将其二值化为前景或背景，或直接映射到调色板后消除中间色。
   - 使用形态学操作（腐蚀/膨胀）清理孤立杂点。
   - 可选：基于连通域分析去除背景噪点。

4. **帧间一致性处理**
   - 对所有帧使用**同一调色板**。
   - 对相邻帧进行颜色匹配，避免因量化导致的闪烁。
   - 可对视频帧进行时间维度上的平滑，但需保持像素硬边缘。

5. **锐化与后处理**
   - 可选：轻度锐化增强边缘，但需避免重新引入抗锯齿。
   - 保持输出为索引色或有限颜色 PNG，确保透明背景（若有）不被破坏。

#### 技术实现建议

- 使用 `Pillow` 的 `resize(..., Image.NEAREST)`、`quantize()`、`convert('P')`。
- 使用 `numpy` 加速像素级操作。
- 使用 `OpenCV` 的形态学操作、连通域分析。

### 10.2 背景去除算法（`background.py`）

**目标**：将图像中接近指定颜色（默认白色）的背景像素设为透明，输出带 Alpha 通道的帧。

**步骤**：
1. 将图像转换为 RGB 或 RGBA。
2. 根据颜色阈值（如 RGB 均大于 240）创建掩膜。
3. 使用形态学操作（开运算/闭运算）清理掩膜边缘。
4. 可选：对边缘半透明像素进行去色处理，减少白边。
5. 将掩膜应用到 Alpha 通道，生成透明背景图像。

---

## 11. 风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 图转视频 API 生成质量不稳定 | 生成动画不符合预期，帧间不一致 | 提供预设提示词模板，优化提示词；允许用户多次生成并选择最佳结果；后处理算法尽量纠正 |
| API 调用成本高 | 用户使用成本上升 | 支持用户自带 API Key，工具不承担费用；提供免费额度和付费订阅模式 |
| 像素化算法效果不佳 | 输出不符合“严格像素”预期 | 持续优化算法参数，提供多种算法选项；允许用户手动微调 |
| 跨平台兼容性问题 | 部分系统无法正常运行 | 使用 PySide6 和标准库，避免平台相关调用；进行多平台测试 |
| 项目复杂度超出预期 | 开发周期延长 | 严格遵循 MVP 原则，优先实现核心功能；模块化设计，便于并行开发 |

---

## 12. 附录：预设提示词库

### 12.1 移动类动作

| 动作 | 英文提示词（示例） |
|------|-------------------|
| 步行 | walking cycle, side view, 8 frames |
| 奔跑 | running cycle, side view, 8 frames |
| 跳跃 | jumping animation, 6 frames |
| 突进 | dashing forward, 6 frames |
| 爬行 | crawling on ground, 8 frames |

### 12.2 战斗类动作

| 动作 | 英文提示词（示例） |
|------|-------------------|
| 攻击 | attack animation, sword swing, 6 frames |
| 格挡 | blocking stance, 4 frames |
| 昏迷 | stunned animation, 4 frames |

> 以上提示词为默认预设，用户可自定义修改和扩展。

---

**文档结束**