# PixelAnimIDE 项目规划（ROADMAP）

> 目标读者：维护者与潜在贡献者。本文档是项目发展的"活文档"，随实现持续更新。
> English version: [ROADMAP.md](ROADMAP.md)。

## 1. 愿景

把「AI 生成 → 像素化 → 精修 → 游戏素材」全流程做成**像素优先的一站式工具**：

- 输入：文本描述 / 自备参考图 / 图生视频
- 产出：像素动画（GIF/APNG/PNG 序列）、精灵图、**地图瓦片与瓦片地图**、可被任意游戏引擎直接使用的素材

## 2. 指导原则

1. **像素优先**：任何处理（缩放 / 键控 / 聚类 / 无缝化）都保证像素锐利、不模糊——"Perfect Pixel" 是本项目的品牌承诺。
2. **可离线演示**：Mock API 永远能跑通全流程，新用户零成本上手。
3. **服务商无关**：API 适配层隔离，新服务商只加配置不改代码。
4. **质量 > 数量**：每个功能做到"像素级完成度"，不堆半成品。
5. **可持续维护**：测试、CI、i18n、文档与功能同步演进。

## 3. 当前状态（v0.3.0）

| 模块 | 状态 |
|------|------|
| Solo 一键流程（文生图→图生视频→像素化→抠图→导出） | ✅ 可用（含参考图 i2i、循环闭合、背景稳定、多服务商适配、尺寸自动回退） |
| IDE 分步工作区（6 步、时间轴、分步参数面板） | ✅ 可用 |
| 像素编辑器（4 工具 + 选择/图层、色族调色板、取色圆盘、导入导出） | ✅ 可用 |
| 精灵图（文生图整张网格 → 裁切 → 抠图 → 导出，同步 IDE） | ✅ 可用 |
| 独立像素板块（分辨率设置、双向同步、图生视频首帧） | ✅ 可用 |
| **瓦片地图模式（第 5 模式，v0.2–v0.3 主线）** | ✅ 可用（见下） |
| 中英 i18n + UI 缩放 + DSH 风格图标 | ✅ 可用 |
| CI（GitHub Actions，Py3.11/3.13 × Win/Linux），**527** 项测试 | ✅ 运行中 |
| Windows 打包（PyInstaller onedir）与 GitHub Release | ✅ v0.3.0 |

### 瓦片地图模式（已完成）

- **地块生态**：一句提示词 → 2×2×3 底图（基础地形 + 3 特征）；自动去格线、检文字（笔画宽度签名）、
  九宫格中值纹理去一次性文字；纹理偏移错位缝合保证**逐像素首尾相等**；**对齐式构图**按掩码派生整族瓦片，
  相邻瓦片共享边逐像素一致；条带深度/分层描边/倒角/环境阴影由 AI 底图**实测**；
  非内部边缘向内不规则噪声；**跨地形/跨瓦片包的块状渗透融合**消除硬边。
- **建筑类**：墙体 **16-tile 族**（直墙/转角/内外角/T 形/十字/端头/孤立块），固定截面几何、
  外部透明可叠加地块、程序化 1px 描边、顶面 + 立面明暗、AI 推导门与立柱件；地图视图带
  **实时自动拼接墙层**（涂/擦即自动选型）。
- **素材（道具）**：变体网格生成 → 按提示词判断主体明暗选择**纯白/纯黑底**并直接按范围删像素 +
  边缘洪泛抠底 → alpha 硬化 0/255 → 裁紧、底部对齐；放置可缩放（25%–400%）。
- **瓦片包与导出**：完整瓦片集目录 + zip（47 图集 8×6、逐张瓦片、
  textures/pieces/props、全部元信息、README）；导入支持文件夹 / zip / `.tilepack`。
- **地图预览**：**无需先生成**即可进入；多包混铺（地块/建筑/素材）、网格开关、Ctrl+左键平移、
  滚轮缩放、画笔/橡皮/旋转/缩放；**柏林噪声大地图**（≤400×400，可散布建筑，瓦片合成按掩码缓存）。
- **布局兼容**：47-tile（8×6 blob 掩码约定）、16-tile 墙体族、**双网格**（dual grid，生成与预览；
  建筑侧为 4 区块墙体族 + 实心件）。
- 64 条核心算法约束以测试固化（共享边逐像素一致、无文字/格线残留、47 类覆盖、透明外部、
  抠底零残留、确定性可复现等）。

已知技术债：打包 onedir 体积大（~250MB）、部分工作流日志未 i18n、GUI 细节测试覆盖不全、
Tiled `.tmx/.tsx` 导入导出未做、瓦片地图图层栈仅“地块 + overlay + 墙体层”三层。

## 4. 阶段规划

### 阶段 A：地图瓦片生成 + 瓦片地图编辑器（v0.2–v0.3 已完成主体 ✅）

**A1 瓦片集生成** ✅ 已完成
- 文生图生成 2×2×3 地块生态底图（基础地形 + 3 特征）与 2×2 建筑底图、素材变体网格，提示词内置
  等大格、纯色底、**可无缝平铺**、风格一致、无文字/边框/格线约束；
- 去格线（期望位置 ± 容差最暗游程 + 残线扫描）、文字检测与修补、偏移错位缝合（wrap-equal）、
  九宫格中值纹理；对齐式构图派生整族瓦片（条带/描边/倒角/阴影实测）；
- 导出 **完整瓦片集目录 + zip**（47 图集 8×6、逐张瓦片、元信息），并兼容 Tiled 常用布局。
- DoD 达成：默认参数下相邻瓦片共享边逐像素一致，长条/长墙无每格接缝。

**A2 瓦片地图编辑器** ✅ 已完成（第 5 模式）
- 瓦片网格画布 + 多包混铺（地块/建筑/素材）、画笔/橡皮/旋转/缩放、网格开关、Ctrl+左键平移；
- **墙体层实时自动拼接**（16-tile 按四邻域选型）、建筑 overlay 层、透明外部叠加；
- **柏林噪声大地图**预览（程序化大陆/河流/山地，可散布建筑）、**双网格**渲染；
- 导出 PNG 预览 + 地图 JSON（overlay 名称/旋转/缩放、墙体层、dual 标记）。
- 待办：Tiled `.tmx/.tsx` 导入导出、洪水填充/矩形/多图层栈。

**A3 与现有管线打通** ✅ 基本完成
- 瓦片包（目录/zip）可在任意预览加载并混铺；地块/建筑/素材三类共用一套包格式；
- 待办：瓦片/地图与 IDE、像素模式的互推（精修回写）。

**A4 大图性能** 🚧 部分完成
- 已做：瓦片合成按（地形艺术 + 掩码）缓存，96×64 大地图秒级出图；跨包融合与墙体层均为向量化实现；
- 待办：视口化渲染、超大图异步导出、瓦片集懒加载。

### 阶段 B：Solo 性能与生成质量优化

**B1 性能**
- **结果缓存**：提示词 / 图片按内容哈希缓存，重复生成直接复用（省 token、省时）；
- **像素化并行**：numpy 向量化 + 多帧线程池/进程池；
- **减 token**：LLM 模板压缩（已有 max_tokens 800）、首帧 min/max side 已有，图片请求尺寸按需降档；
- 大图预览 / 导出异步化，进度粒度细化。

**B2 质量**
- **多候选生成**：`n=2~4` 生多张，按客观评分（清晰度 / 网格纯度 / 主体完整度）自动选优，UI 允许用户挑选；
- **提示词模板升级**：few-shot 示例 + 参数化风格预设（像素风 / 色板 / 描边强度）；
- **视频一致性**：抽帧后逐帧颜色直方图匹配、更好的循环首尾检测；
- **抠图升级**：可选描边处理、边缘抗锯齿、羽化增强；
- **seed 控制**：可复现生成。

**B3 质量回归基准**
- 建立示例图集 + 客观评分脚本，任何改动跑基准防质量回退（并入 CI 可选 job）。

### 阶段 C：像素编辑器 & 精灵图精进

**C1 像素编辑器**
- 更多工具：直线 / 矩形 / 椭圆 / 对称绘制 / 魔棒选择 / 像素字体文本；
- **真实图层栈**（替代当前单浮动层）+ 图层面板；
- 编辑页内嵌动画预览（帧序列直接播放）、洋葱皮强度调节；
- 快捷键自定义、图案笔刷。

**C2 精灵图**
- **页内逐帧编辑**（点格子直接进编辑器，无需先同步 IDE）；
- 裁切优化：内容包围盒 + 留白参数化、去边框参数 UI；
- 多动作精灵图（一个 sheet 多行动作）、碰撞盒标注；
- 生成结果多候选挑选。

### 阶段 D：持续打磨（贯穿始终）

- **D1 工程**：PyInstaller onefile + 图标 + 版本自检；自动更新（可选）；CI 加打包 job 与 codecov；
- **D2 体验**：空状态引导、快捷键帮助、设置项精简、错误提示友好化、日志分级与搜索；
- **D3 国际化**：英文文案 100% 覆盖、日语等更多语言包、字体适配；
- **D4 文档**：用户手册（中英）、示例画廊、API 适配文档、FAQ；
- **D5 社区**：Issues 模板、Contributing、首个外部贡献。

## 5. 里程碑

| 里程碑 | 范围 | 状态 |
|--------|------|------|
| **M1（v0.2）** | A1 瓦片集 + A2 瓦片地图 MVP + 打包 | ✅ 已发布 |
| **M2（v0.3）** | 47/16/双网格算法精修、建筑 16-tile 族、素材（道具）链路、瓦片包导入导出、交界渗透融合、柏林噪声大地图、免生成预览 + 截图画廊 | ✅ 已发布 |
| **M3** | Tiled `.tmx/.tsx` 导入导出、地图图层栈与矩形/填充工具、A3 精修回写、A4 视口化/异步导出 | 下一版本 |
| **M4** | B1 结果缓存/并行 + B2 多候选与模板升级 + C2 精灵图内联编辑 | M3 之后 |
| **M5** | 稳定打磨、100% i18n、D1 自动更新、社区运营、**v1.0** | 稳定发布 |

## 6. 优先建议（Quick Wins，先做收益最高的）

1. Tiled `.tmx/.tsx` 导入导出——让导出物直接进入主流关卡编辑器；
2. 地图图层栈与矩形/填充/魔棒——地图编辑从“预览”走向“可用编辑器”；
3. Solo 结果缓存与像素化并行——用户最直接的省时省钱；
4. 编辑页动画预览 + 洋葱皮强度——编辑器日常体验提升；
5. 多候选生图——质量感知最大提升点。

## 7. 如何跟踪

- 建议在 GitHub 建立 **Milestones（M1–M4）** 与 **Labels**：`tiles` / `map-editor` / `solo-quality` / `performance` / `pixel-editor` / `sprite` / `i18n` / `packaging` / `docs` / `good-first-issue`；
- 每个 PR 关联对应 Issue，里程碑按此文档拆解；
- 质量底线：任何改动必须跑通 `pytest`（当前 527 项）且不引入回归。

---

*最后更新：2026-09-17（随 v0.3.0 发布更新：瓦片地图模式主体完成）*
